{
  "name": "postman-newman-jenkins",
  "version": "1.0.0",
  "description": "Project for storing Postman Tests",
  "directories": {
    "test": "tests"
  },
  "scripts": {
    "api-test": "newman run tests/httpbin.postman_collection.json --environment=environment/httpbin.postman_environment.json --reporters junit --reporter-junit-export newman.xml"
  },
  "author": "Manpreet",
  "dependencies": {
    "newman": "^5.0.0"
  }
}
